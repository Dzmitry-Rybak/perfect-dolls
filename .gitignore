node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example

# Референсы — палитра с них уже снята в tokens.css
styleILike/

# Исходники графики. Нарезанные и сжатые версии лежат в public/art —
# сами листы весят 5.6 МБ и в репозитории не нужны (из-за такого же
# веса уже падал push с HTTP 400).
images-png/
toys-squidy/

# Фотографии и скриншоты, присланные Маргаритой, — рабочие референсы.
# Код на них не ссылается, обводки с них уже сняты в src/data/squidArt.js
# и squidParts.js. 14 МБ HEIC и PNG в репозитории не нужны.
fresh/
