/**
 * Картинка одного раздела на главной: снимок и второй кадр,
 * который проявляется при наведении мышкой.
 *
 * Отдельный тип, потому что таких блоков на главной четыре —
 * сквиды, куклы, портреты и «другое», и все устроены одинаково.
 */
export default {
  name: 'homeCard',
  title: 'Картинка раздела',
  type: 'object',
  fields: [
    {
      name: 'photo',
      title: 'Снимок',
      type: 'image',
      options: { hotspot: true },
      description:
        'Кадр обрезается до вертикального прямоугольника. Если обрезало не то — ' +
        'нажмите на снимке кнопку с рамкой (Crop / Hotspot) и перетащите кружок ' +
        'на то, что должно остаться в кадре.',
      fields: [
        {
          name: 'alt',
          title: 'Что на снимке',
          type: 'string',
          description: 'Одна фраза для тех, кто не видит картинку.',
          validation: (R) => R.max(160),
        },
      ],
    },
    {
      name: 'photoHover',
      title: 'Второй кадр (при наведении)',
      type: 'image',
      options: { hotspot: true },
      description:
        'Необязательный. На компьютере карточка плавно перетекает на него, ' +
        'когда наводят мышкой. На телефоне не показывается и не скачивается.',
    },
  ],
  preview: {
    select: { media: 'photo' },
    prepare: ({ media }) => ({ title: media ? 'Снимок выбран' : 'Пусто', media }),
  },
};
